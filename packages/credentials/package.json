{
  "name": "@anon/credentials",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "cd src/circuit && nargo compile && bb write_vk -b target/main.json -o target/vkey",
    "test": "bun test",
    "test:circuit": "cd src/circuit && nargo test"
  },
  "dependencies": {
    "@aztec/bb.js": "^0.61.0",
    "@noir-lang/noir_js": "^0.38.0",
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
